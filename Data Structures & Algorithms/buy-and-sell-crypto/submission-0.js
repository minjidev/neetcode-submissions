class Solution {
    /**
     * @param {number[]} prices
     * @return {number}
     */
    maxProfit(prices) {
        // 각 위치에서 이제까지의 최소값과 비교한다. 
        for (let i=0;i<prices.length;i++) {
            
        }
    }
}
